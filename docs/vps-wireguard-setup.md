# WireGuard VPS setup (moved)

This backend doc is now intentionally minimal to avoid duplicating VPS deployment guidance.

Use the canonical runbook in the edge manager repo:

- `mira-vpn-wgmgr/docs/vps-deploy.md`

Use this backend repo doc for control-plane behavior and location mapping:

- [wireguard-locations.md](./wireguard-locations.md)
